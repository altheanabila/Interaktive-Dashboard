[build]
builder = "nixpacks"
dockerfilePath = "backend/Dockerfile"

[deploy]
startCommand = "java -jar backend/target/InteraktiveDashboard-1.0-SNAPSHOT.jar --server.port=$PORT"

[deploy.healthcheck]
port = 8080
path = "/actuator/health"